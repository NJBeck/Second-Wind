#pragma once

#include <glm/glm.hpp>
#include <cstdint>

typedef uint64_t EntityID;
typedef uint32_t u32;
typedef uint64_t u64;
typedef int32_t i32;
typedef int64_t i64;
typedef glm::vec3 Pos;	// xyz of object position
typedef glm::vec3 Dims;	// xyz of dimensions of object
typedef glm::vec3 Dir;	// xyz of direction vector